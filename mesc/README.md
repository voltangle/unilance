# MESC

This crate is a heavily modified version of the MESC_Firmware project created by David
Molony (aka mxlemming). A lot of components from the original project were removed (for
example only MESC_Common was brought over from the project), and the entire configuration
system was rewritten to not use configfile-style configuration system and instead be
fully configurable at runtime.
