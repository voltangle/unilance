[workspace]
resolver = "3"
members = ["mesc", "core_control", "core_supervisor", "port_stm32f4", "proto", "proc_macros"]

[profile.release]
lto = true
opt-level = "s"
incremental = false
codegen-units = 1
# note: debug = true is okay - debuginfo isn't flashed to the device!
debug = true
