[workspace]
resolver = "3"
members = ["mesc", "core", "port_stm32f4"]

[profile.release]
# lto = true
opt-level = "s"
incremental = false
codegen-units = 1
# note: debug = true is okay - debuginfo isn't flashed to the device!
debug = true
