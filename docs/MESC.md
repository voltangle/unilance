> This is just unordered notes. When there will be enough notes, make them organized

`MESCfoc_Init` is effectively moved to Rust code to the `mesc::Motor` struct. All setup
is done at runtime without any constants or defines in the `mesc` crate.
